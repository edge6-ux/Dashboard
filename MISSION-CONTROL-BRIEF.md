# Mission Control — Project Brief for Gordon

## Project Overview
- **Project:** Mission Control — Edge's personal AI command center
- **Mission:** A unified dashboard to manage agents (Christopher & Gordon), track tasks, view scheduled jobs, and monitor everything from one place
- **Location:** `C:\Users\edger\Documents\dashboard\`

## Tech Stack & Infra
- **Frontend:** Single-file HTML/CSS/JS (`dashboard.html`) — dark theme, Inter + JetBrains Mono fonts
- **Data:** `tasks.json` (shared between agents and dashboard), localStorage for UI-only state
- **Backend/Platform:** OpenClaw gateway (port 18789) — manages agents, cron jobs, channels
- **Agents:**
  - Christopher (🧠) — Claude Opus 4, primary agent, webchat + TUI
  - Gordon (⚡) — GPT-5 Mini, handles Discord/Telegram/WhatsApp/Signal
- **Channels:** Discord (bot), Telegram (bot), webchat, TUI
- **Cron:** OpenClaw cron system for scheduled jobs
- **Config:** `C:\Users\edger\.openclaw\openclaw.json`
- **Gordon workspace:** `C:\Users\edger\.openclaw\workspace-gordon\`
- **Christopher workspace:** `C:\Users\edger\.openclaw\workspace\`
- **Dashboard folder:** `C:\Users\edger\Documents\dashboard\`
- **Themes (Control UI):** `C:\Users\edger\.openclaw\workspace\themes\`

## Current Status

### ✅ Done
- Command Center dashboard built (`dashboard.html`) — stats, calendar, jobs, task tracker, agent cards
- Gordon model fixed — now running GPT-5 Mini (was silently falling back to Opus)
- Daily Briefing cron jobs working — 8:30 AM ET weekdays, delivers to Discord
- Brave Search API key configured for web search
- Task system: agents can create tasks via `tasks.json`, dashboard reads them
- Schedule builder UI — friendly dropdowns, no cron syntax required
- "Just Once" task support for one-time jobs
- Task Tracker page with status filters (queued/running/completed/error)
- Agent dashboard page (`agents.html`) with status cards
- Calendar page (`calendar.html`) with events

### 🔄 In Progress
- Dashboard refinement in Cursor — Edge is making UI tweaks
- Task creation from Discord (Gordon can now create tasks via `tasks.json`)

### 🚫 Not Yet Started
- Supabase integration (database backend for tasks — currently file-based)
- Live data from OpenClaw API (dashboard currently uses hardcoded + localStorage data)
- Authentication / multi-device access
- Real-time agent status (online/offline/idle) from gateway
- Push notifications for task updates

## Top Priorities (Ranked)
1. **Supabase setup** — move tasks from `tasks.json` to a real database
2. **Dashboard stability** — fix any remaining UI bugs from recent changes
3. **Live data integration** — pull real job/agent status from OpenClaw gateway API
4. **Task creation flow** — Gordon creates tasks from Discord chat, they show on dashboard
5. **Polish & deploy** — get dashboard accessible from anywhere (not just local file)

## Known Constraints
- **Token budget:** Gordon runs on GPT-5 Mini to save costs. Christopher uses Opus 4 — don't change this
- **Brave API:** $5/month free tier — sufficient for daily briefings, don't run excessive searches
- **OpenClaw config:** Changes to `openclaw.json` require gateway restart (`openclaw gateway restart`)
- **Session cache:** Changing agent models requires clearing sessions (`agents/<agent>/sessions/`) + restart
- **File:// limitation:** Dashboard can't fetch `tasks.json` when opened as local file — needs Live Server or HTTP serving

## Where Gordon Should Start
- **Triage tasks:** Review `tasks.json` and help Edge track what needs doing
- **Create tasks from Discord:** When Edge says "create a task" or "track this", write to `tasks.json`
- **Daily Briefing:** This is yours — runs every weekday at 8:30 AM, keep it sharp
- **Prompt support:** Help Edge draft prompts for Cursor when working on the dashboard
- **Test deploys:** When dashboard changes land, verify they work

## Do Not Touch
- Christopher's workspace files (`SOUL.md`, `IDENTITY.md`, `USER.md`, `MEMORY.md`)
- OpenClaw config (`openclaw.json`) — ask Christopher or Edge first
- Auth profiles / API keys — don't modify without explicit approval
- Discord bot token or channel configs

## Task File Format
**Path:** `C:\Users\edger\Documents\dashboard\tasks.json`

```json
{
  "id": "task-<timestamp-hex>",
  "name": "Short title",
  "agent": "gordon" or "christopher",
  "status": "queued|running|completed|error",
  "type": "one-time" or "recurring",
  "description": "Details...",
  "createdAt": <ms>, "updatedAt": <ms>, "completedAt": <ms or null>,
  "color": "#06b6d4",
  "schedule": "", "error": null,
  "links": [{"url": "...", "label": "..."}],
  "documents": [{"name": "file.pdf"}],
  "progress": 0-100
}
```

---
*Generated by Christopher — March 22, 2026*
